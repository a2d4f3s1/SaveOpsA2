schema_version = "1.0.0"

id = "saveopsa2"
version = "1.2.6"
name = "SaveOpsA2"
tagline = "Blend file backups on save and timed auto-backup copies"
maintainer = "a2d4f3s1"
type = "add-on"

tags = ["System"]

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Create and rotate blend file backups next to your project files"

[build]
paths_exclude_pattern = [
  "__pycache__/",
  "*.zip",
]
